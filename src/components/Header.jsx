import React from 'react';

export default function Header() {
  return (
    <div>
      <h2>Once Upawn a Time</h2>
      {/* //this buton logs out current user, still needs logout function */}
      <button>logout</button>
    </div>
  );
}
